


<br/><br/>
<center><h1>Listing zones</h1></center>

<table id="table_id"> 
<thead> 
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
</thead>
<tbody>
<% @zones.each do |zone| %>
  <tr>
    <td><%= zone.name %></td>
    <td><%= link_to 'Show', zone %></td>
    <td><%= link_to 'Edit', edit_zone_path(zone) %></td>
    <td><%= link_to 'Destroy', zone, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</tbody
</table>

<br />

<%= link_to 'New Zone', new_zone_path %>
